#include <stdio.h>
#include <math.h>

int main()
{
	
    float num, raiz;

        printf("Digite um numero: ");
        scanf("%f",&num);

        raiz = sqrt(num);

        printf("Raiz quadrada = %.2f",raiz);

return 0;

}
